{
  "name": "component-transformer-test",
  "version": "1.0.0",
  "package": "tla.EAM",
  "description": "transform classes to reflective components test classes",
  "main": "dist/main.js",
  "scripts": {
    "prepare": "ts-patch install",
    "start": "cd .. && npx tsc && cd transformer-test && npx tsc",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.6",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "reflect-metadata": "github:rbuckton/reflect-metadata",
    "rimraf": "^3.0.2",
    "ts-patch": "^2.0.1"
  }
}